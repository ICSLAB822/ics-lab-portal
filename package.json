{
  "name": "ics-lab-portal",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "homepage": "https://ICSLAB822.github.io/ics-lab-portal",
  "scripts": {
    "build:css": "tailwindcss -i styles/tailwind.css -o assets/css/tailwind.css --minify",
    "watch:css": "tailwindcss -i styles/tailwind.css -o assets/css/tailwind.css --watch",
    "build": "npm run build:css && bundle exec jekyll build",
    "dev": "npm run build:css && bundle exec jekyll serve",
    "validate": "bundle exec ruby scripts/validate_content.rb && bundle exec ruby scripts/validate_site.rb && bundle exec ruby scripts/check_links.rb",
    "check": "npm run build && npm run validate && npm run test:baseurls && npm test && npm run test:announcement",
    "test": "playwright test",
    "test:smoke": "node scripts/smoke-site.cjs",
    "test:announcement": "node scripts/test-announcement.cjs",
    "test:baseurls": "bundle exec ruby scripts/check_baseurls.rb",
    "audit:production": "npm audit --omit=dev --audit-level=moderate"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.13.0",
    "@playwright/test": "1.58.2",
    "tailwindcss": "3.4.17"
  }
}
